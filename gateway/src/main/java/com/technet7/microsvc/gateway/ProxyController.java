package com.technet7.microsvc.gateway;

import org.springframework.stereotype.Component;

/**
 * Placeholder ProxyController class.
 *
 * The original servlet-based proxy was removed when converting the gateway to use
 * Spring Cloud Gateway (reactive). This placeholder remains to avoid accidental
 * re-introduction of servlet APIs in the gateway module.
 */
@Component
public class ProxyController {
    // Intentionally empty - Spring Cloud Gateway handles routing now.
}
