<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ai.catalog">

    <!-- Debug manifest overlay: enable cleartext traffic for debug builds -->
    <application
        android:usesCleartextTraffic="true" >

        <!-- Debug-only helper activity to exercise registration via adb -->
        <activity
            android:name=".debug.TestRegistrationActivity"
            android:exported="true" />

    </application>

</manifest>
