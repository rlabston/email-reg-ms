<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.ai.catalog">

    <!-- Debug manifest overlay: enable cleartext only for debug builds via a network security config -->
    <application
        android:networkSecurityConfig="@xml/network_security_config_debug"
        android:usesCleartextTraffic="true" >

        <!-- Debug-only helper activity to exercise registration via adb -->
        <activity
            android:name=".debug.TestRegistrationActivity"
            android:exported="true" />

    </application>

</manifest>
